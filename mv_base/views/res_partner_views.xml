<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<!-- ACTIONS (SERVER) -->

	<record id="mv_base_model_res_partner_action_recompute_methods" model="ir.actions.server">
        <field name="name">Recompute Partners</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_recompute_methods()</field>
    </record>

</odoo>
