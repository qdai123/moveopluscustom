<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_order_form_mv_sale" model="ir.ui.view">
            <field name="name">view_order_form_mv_sale</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[@name='order_details']" position="inside">
                    <field name="check_discount_10" invisible="1"/>
                    <field name="bonus_order" invisible="1"/>
                    <field name="date_invoice" readonly="0" invisible="0"/>
                </xpath>
                <xpath expr="//header" position="inside">
                    <field name="flag_delivery" invisible="1"/>
                    <button name="action_compute_discount_month" id="action_compute_discount_month" data-hotkey="q" string="Chiết khấu tự động" class="btn-primary" type="object" context="{'validate_analytic': True}" invisible="state not in ['sent', 'draft'] or flag_delivery"/>
                    <button name="action_compute_discount_month" id="action_compute_discount_month" data-hotkey="q" string="Nhập chiết khấu sản lượng" class="btn-primary" type="object" context="{'validate_analytic': True}" invisible="state not in ['sent', 'draft'] or not flag_delivery"/>
                </xpath>
            </field>
        </record>

        <record id="sale_order_tree_mv_sale" model="ir.ui.view">
            <field name="name">sale_order_tree_mv_sale</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.sale_order_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//tree" position="inside">
                    <field name="date_invoice" optional="hide"/>
                </xpath>
            </field>
        </record>

    </data>
</odoo>